// Placeholder — the search UI is built in Phase 6.
export default function SearchView() {
  return (
    <main style={{ padding: "2rem", fontFamily: "var(--font-display)" }}>
      <h1>Sanctions Entity Explorer</h1>
    </main>
  );
}
